name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy-convex:
    name: Deploy Convex
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Deploy Convex
        run: npx convex deploy
        env:
          CONVEX_DEPLOY_KEY: ${{ secrets.CONVEX_DEPLOY_KEY }}

  deploy-knowledge:
    name: Deploy Knowledge Engine
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Check for knowledge-engine changes
        id: changes
        run: |
          if git diff --name-only HEAD~1 HEAD | grep -q '^services/knowledge-engine/'; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Fly.io CLI
        if: steps.changes.outputs.changed == 'true'
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Deploy Graphiti (main) to Fly.io
        if: steps.changes.outputs.changed == 'true'
        run: cd services/knowledge-engine && flyctl deploy -c graphiti-main.fly.toml
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

  deploy-deck:
    name: Deploy Deck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Check for deck changes
        id: changes
        run: |
          if git diff --name-only HEAD~1 HEAD | grep -q '^apps/deck/'; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/setup-node@v4
        if: steps.changes.outputs.changed == 'true'
        with:
          node-version: 20

      - name: Install and build
        if: steps.changes.outputs.changed == 'true'
        run: cd apps/deck && npm install && npm run build

      - name: Deploy to Cloudflare Pages
        if: steps.changes.outputs.changed == 'true'
        run: cd apps/deck && npx wrangler pages deploy dist --project-name openping-deck --branch main --commit-dirty=true
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
